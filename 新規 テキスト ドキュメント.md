南山大学人文学部3年の長谷川壮流です！
文系の学生として新卒でエンジニアを目指す僕が学んだことなどをここにメモしていきます。💻

〇改善提案アプリ
・技術選定の理由
学習している技術スタックは限られていますが、HTML・CSS・JavaScriptにはWebブラウザさえあれば動作し、インストール不要で誰でも利用できるという利点があります。
また、アプリではなくWeb技術を使うことで、多様な環境でも簡単に展開できると判断し、これらの技術を選定しました。

・工夫したこと
　従業員側の画面をChatGPTの初期画面のようにしたこと。理由として、対象ユーザーがハードワークをこなしたあとの社会人であるため、シンプルなUIにすることで視覚的なストレスを与えないように設計しました。具体的にはCSSのjustify-content: center;とaling-items: center;を用いてテキストやフォームを中央に配置しました。

・学んだこと
　.envファイルを使ってAPIキーを保護してからプッシュしなければならないこと。→セキュリティの点から。
　
　async（非同期処理）に対して、awaitは処理を待たなければならないこと。→データの送信状況を見てからでないと「送信成功・失敗」の判断はできないから。

〇写経（計算機・タイマー・To Doリスト）
・工夫したこと
　ルートの計算をできるようにしたこと。jsでの計算部分に

exp = exp.replace(/√(\d+(\.\d+)?)/g, "Math.sqrt($1)");
    while (exp.match(/√(\d+(\.\d+)?)/)) {
        exp = exp.replace(/√(\d+(\.\d+)?)/g, "Math.sqrt($1)");
    }

を加えてルートの数字を「Math.sqrt(数字)」に変換して計算できるようにしました！

・学んだこと
ドット記法→オブジェクトの階層構造を.でつなげるもの！（基本）